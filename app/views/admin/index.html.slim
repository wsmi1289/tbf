section#admin
  .row
    .col
      h1 Welcome
    .col
      p Total Billable #{number_to_currency(@total_sales)}
    .col
      a.btn.btn-light.mr-3 href=new_page_path New Page
      a.btn.btn-light href=categories_path Categories
  table
    thead
      tr
        th Email
        th Admin
        th Client
    tbody.user-table
      - @users.each do |u|
        = render 'user_row', user: u