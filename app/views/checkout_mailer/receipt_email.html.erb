<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <ul>
      <h3>Your Order</h3>
    <%- @cart.line_items.each do |line_item| %>
      <li>
        <%= line_item.quantity %> &times; <%= line_item.product.title %>
        <%= number_to_currency(line_item.total_price) %>
      </li>
    <% end %>
      <p>Total: <%= number_to_currency(@cart.total_price) %></p>
      <p>Please have cash or check ready on delivery</p>
    <%- if @orderer.present? %>
      <p>Ordered By: <%=@orderer%></p>
    <% end -%>
    </ul>
  </body>
</html>